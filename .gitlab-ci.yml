image: "$CI_REGISTRY_IMAGE/alpine-mono"

stages:
  - builds
  - tests

test:
  stage: tests
  script:
    - echo "Test"

test2:
  stage: tests
  script:
    - echo "Test2"

build:
  stage: builds
  script:
    - xbuild build.csproj
