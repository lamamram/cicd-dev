image: "$CI_REGISTRY_IMAGE/alpine-mono"

stages:
  - builds
  - tests

test:
  stage: tests
  script:
    - echo "Test"

# désactiver un job en préfixant par "."
build:
  stage: builds
  # clé pour placer des commandes de préparation du job
  # before_script:
  #   - apt-get install -y mcs
  script:
    - xbuild build.csproj
